class Solution:
    def findDuplicate(self, nums: List[int]) -> int:
        search = 1
        while True:
            i = 0
            while i < (len(nums) - search):
                if(nums[i] == nums[i+search]):
                    return nums[i]
                    braek
                i += 1
            search += 1
