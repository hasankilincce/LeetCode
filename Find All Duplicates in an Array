class Solution:
    def findDuplicates(self, nums: List[int]) -> List[int]:
        nums.sort()
        i = 1
        result = []
        while i < len(nums):
            if nums[i-1] == nums[i]:
                result.append(nums[i])
            i += 1
        return result
